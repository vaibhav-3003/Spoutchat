<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SpoutChat" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="android:colorPrimary">@color/primary</item>
        <item name="colorPrimary">@color/primary</item>
        <!-- Secondary brand color. -->
        <item name="colorOnSecondary">@color/black</item>
        <!-- Customize your theme here. -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:editTextColor">@color/secondary_text</item>
        <item name="android:textColorHint">@color/secondary_text</item>
        <item name="searchViewStyle">@style/MySearchView</item>
        <item name="autoCompleteTextViewStyle">@style/cursorColor</item>

    </style>

    <style name="progressBarColor" parent="@style/Theme.AppCompat">
        <item name="colorAccent">@color/primary</item>
    </style>
    <style name="progressBarColor2" parent="@style/Theme.AppCompat">
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Widget.App.BottomNavigationView" parent="Widget.Material3.BottomNavigationView">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.BottomNavigationView</item>
    </style>

    <style name="ThemeOverlay.App.BottomNavigationView" parent="">
        <item name="colorSurface">@color/primary</item>
        <item name="colorOnSurfaceVariant">@color/colorAccent</item>
    </style>


    <style name="MySearchView" parent="Widget.AppCompat.Light.SearchView">

        <item name="closeIcon">@drawable/round_cancel_24</item>
        <item name="android:queryBackground">@android:color/transparent</item>
        <item name="searchHintIcon">@null</item>
        <item name="searchIcon">@null</item>

    </style>

    <style name="cursorColor" parent="Widget.AppCompat.AutoCompleteTextView">
        <item name="android:textCursorDrawable">@drawable/cursor_background</item>
    </style>

    <style name="ShapeAppearanceOverlay.Circle" parent="">
        <item name="cornerSize">@dimen/_30sdp</item>
    </style>

    <style name="MyToolbarTitleStyle" parent="@style/TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">20sp</item>
        <!-- Additional text appearance attributes can be added here -->
    </style>

</resources>